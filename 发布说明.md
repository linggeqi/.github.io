# 黄世宝 · 个人网站（GitHub Pages 部署包）

## 目录结构
```
index.html            主页（图片路径已改为同目录 images/，可直接静态托管）
黄世宝_简历.pdf        下载用简历
images/               首页/各板块背景与过渡静帧（25 张，约 328MB）
网站配图/opt/…         静帧展区轮播图（10 组 × 5 张，约 6.4MB）
发布说明.md            本文件（可删除后上传）
```

## 部署到 GitHub Pages

> 提示：包内有 5 张约 34MB 的背景 PNG，GitHub 网页版上传单个文件上限 25MB，
> 所以请用 git 推送（GitHub Desktop 或命令行），不要用网页拖拽上传。

命令行方式（在你自己的电脑终端执行，把 USER / REPO 换成你的账号和仓库名）：

```bash
cd 解压后的目录（即本目录）
git init
git add .
git commit -m "deploy: personal resume site"
git branch -M main
git remote add origin https://github.com/USER/REPO.git
git push -u origin main
```

然后在 GitHub 仓库：Settings → Pages → Source 选 `Deploy from a branch`，
分支 `main`、目录 `/ (root)` → Save。约 1 分钟后访问
`https://USER.github.io/REPO/` 即可。

## 建议（可选）：压缩图片再上传
328MB 图片会让首次加载变慢。建议发布前压缩一次并重新 git push：

- 免费工具：squoosh.app、tinypng.com 或「图压」
- 建议：把背景大图等比缩到宽 1920–2560px、质量 80 左右，单张控制在 1–3MB；
- 压缩后放到与现在相同的路径（同名覆盖 `images/` 与 `网站配图/…`），
  index.html 无需改动。

## 安全与隐私
- 本包已剔除所有本机路径、无关文件和本地工程内容，仅含网站资源与简历 PDF；
- 页面中的姓名、电话、邮箱为你的公开联系方式，属预期展示内容；
- 不要在 git 仓库里提交任何密钥/Token/本机配置；部署后仓库若设为 Public，
  网站即公网可见，请确认以上公开信息为有意公开。
