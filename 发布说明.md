# 黄世宝 · 个人网站（GitHub Pages 部署包）

> **本包版本：20260919-改3**
>
> **改3：方法板块版式（本次）**
> 1. **展开区改为通栏** —— 原先展开的中英文步骤只挤在右侧一列里（占整行右侧约 45%），
>    现在展开区 `grid-column: 1 / -1` 铺满整行，中文落在左侧、英文落在右侧。
> 2. **中英分栏比例 35% : 65%** —— `grid-template-columns: minmax(0, 0.70fr) minmax(0, 1.30fr)`。
>    中文步骤右边界固定在行宽的 35%（1280 窗口下 x446.5），英文从该处起。
> 3. **两条列表各自左右对称** —— 中文 `ol` 左右各 30px 内距；
>    英文 `ol` 左右同值 `clamp(14px, 2vw, 28px)`（它在分界右侧还自带一段内距，
>    所以取较小值才能保证自身居中且不贴分界）。
> 4. 展开/折叠动画、无裁切两项在改3 中保持不变（仍为 `height` 过渡 + 实测落位高度）。
>
> **改2：方法板块开合动画修复**
> 改1 把高度限制换成 `grid-template-rows: 0fr → 1fr` 过渡，但部分浏览器不插值
> 网格行轨道，结果整段「啪」地跳开、缓入缓出动画完全消失。现改为对 `height`
> 做过渡（所有引擎都必然插值），展开落位高度由 JS 按真实内容实测后写入
> `--flow-hopen`，并预留 2px 抗亚像素舍入。实测曲线（560ms）：
> 展开 `0 → 7.7 → 37.5 → 110.3 → 265 → 419.7 → 492.5 → 522.3 → 530`，
> 折叠为其反向；504 / 800 / 1024 / 1280 / 1440 五档宽度全部零裁切。
> 手机档仍为「全部展开、无动画」（一行一屏，无需开合）。
>
> **改1：原始 5 项修改**
> 1. **方法板块内容不再被裁切** —— 原 `.flow-detail` 用 `max-height: 380px / 520px` 限高，
>    第 2、4 条正文（自然高 530 / 577px）会被切掉后半段。现按真实内容高度展开，
>    任意长度文案都能完整显示。
> 2. **右侧引导键「SOP」改为「方法」**（桌面侧栏文案与无障碍标签同步；手机底栏本就是「方法」）。
> 3. **手机端静帧图组重做** —— 原先手机上看不到照片（`.frame-visual` 恒为 `display:none`，
>    只剩 5 张缩略色块叠在一起）。现每个图组就是一屏：**上下滑更换组内 5 张图，左右滑切换图组**
>    （横向用 CSS `scroll-snap` 原生滚动，不会被脚本吞掉手势），右上角计数徽章显示 `组号 · 第几张/5`。
> 4. **自动轮换改为 10 秒一换**（原 8.2 秒）。手机上先走完当前组的 5 张图，再自动切到下一组。
> 5. **手机端底部留白收紧** —— 底部检测条由 30–44px 压到 20px，底栏保留高度由 76px 收到 62px，
>    页面底部的死区由 145.8px 降到 0。
> 6. **修复底部抽屉 bug** —— 收起抽屉后再也无法上下滑动页面。原因是触摸手势一旦被抽屉接管，
>    就无条件 `preventDefault()` 并锁死。现改为「先判方向再决定是否接管」：手指上滑/下滑翻页一律放行，
>    只有明确的下拉动作才由抽屉接管，且释放时清空所有内联样式。

## 目录结构
```
index.html            主页（图片路径已是站内相对路径，可直接静态托管）
黄世宝_简历.pdf        页面“下载简历”按钮对应的文件
images/               首屏与各板块的背景/过渡静帧（25 张，长边 2560 JPEG，约 11MB）
网站配图/web/…         静帧展区轮播图（10 组 × 5 张，长边 1920 JPEG，约 8.7MB）
发布说明.md            本文件（上传前可删除）
```
整包约 15MB，所有文件均小于 GitHub 单文件 25MB 的网页上传上限。

## 部署到 GitHub Pages

### 方式一：网页上传（最省事）
1. GitHub 新建仓库（建议 Public）；
2. 进入仓库 → Add file → Upload files，把本目录里的 `index.html`、`黄世宝_简历.pdf`、
   `images/`、`网站配图/` 一并拖进去（保留文件夹结构）→ Commit；
3. Settings → Pages → Source 选 `Deploy from a branch`，Branch 选 `main`、目录 `/ (root)` → Save；
4. 约 1 分钟后访问 `https://<账号>.github.io/<仓库名>/`。

### 方式二：命令行（更稳，推荐文件多时使用）
```bash
cd 本目录
git init
git add .
git commit -m "deploy: personal resume site"
git branch -M main
git remote add origin https://github.com/<账号>/<仓库名>.git
git push -u origin main
```
然后同样在 Settings → Pages 里开启 main / root。

## 说明
- 背景图与轮播图均已按网页用途压缩（原图 400MB+ → 站内约 20MB），源图仍在本地保留；
- 页面中的姓名、电话、邮箱为公开联系方式，属预期展示内容；
- 仓库若设为 Public，网站即公网可见；请勿把任何密钥/Token/本机配置提交进仓库。
