# 黄世宝 · 个人网站（GitHub Pages 部署包）

## 目录结构
```
index.html            主页（图片路径已是站内相对路径，可直接静态托管）
黄世宝_简历.pdf        页面“下载简历”按钮对应的文件
images/               首屏与各板块的背景/过渡静帧（25 张，长边 2560 JPEG，约 11MB）
网站配图/web/…         静帧展区轮播图（10 组 × 5 张，长边 1920 JPEG，约 8.7MB）
发布说明.md            本文件（上传前可删除）
```
整包约 15MB，所有文件均小于 GitHub 单文件 25MB 的网页上传上限。

## 部署到 GitHub Pages

### 方式一：网页上传（最省事）
1. GitHub 新建仓库（建议 Public）；
2. 进入仓库 → Add file → Upload files，把本目录里的 `index.html`、`黄世宝_简历.pdf`、
   `images/`、`网站配图/` 一并拖进去（保留文件夹结构）→ Commit；
3. Settings → Pages → Source 选 `Deploy from a branch`，Branch 选 `main`、目录 `/ (root)` → Save；
4. 约 1 分钟后访问 `https://<账号>.github.io/<仓库名>/`。

### 方式二：命令行（更稳，推荐文件多时使用）
```bash
cd 本目录
git init
git add .
git commit -m "deploy: personal resume site"
git branch -M main
git remote add origin https://github.com/<账号>/<仓库名>.git
git push -u origin main
```
然后同样在 Settings → Pages 里开启 main / root。

## 说明
- 背景图与轮播图均已按网页用途压缩（原图 400MB+ → 站内约 20MB），源图仍在本地保留；
- 页面中的姓名、电话、邮箱为公开联系方式，属预期展示内容；
- 仓库若设为 Public，网站即公网可见；请勿把任何密钥/Token/本机配置提交进仓库。
